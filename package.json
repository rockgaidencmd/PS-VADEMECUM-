{
  "name": "vademecum-enfermeria",
  "version": "1.0.0",
  "description": "Vademécum Enfermería — consulta farmacológica para profesionales de enfermería",
  "private": true,
  "scripts": {
    "build": "node scripts/build-www.js",
    "sync": "npm run build && cap sync android",
    "aab": "npm run sync && cd android && ./gradlew bundleRelease",
    "apk": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
